name: Keep Alive — mhida.org

on:
  schedule:
    - cron: '0 3 */3 * *'   # every 3 days at 3:00 AM UTC
  workflow_dispatch:          # allow manual trigger anytime

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping mhida.org
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}" https://mhida.org
          echo " — site is alive"
